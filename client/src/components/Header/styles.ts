import { ElementStyles } from 'src/types';

export const styles: ElementStyles.ListStyles = {
  wrapper: {
    flexGrow: 1,
  },

  //   box: (theme) => ({
  //     color: theme.palette.primary.main,
  //   }),
};
